# FIXED: Added the mandatory space after the dash '-'
# This was causing the deployment task to fail immediately.
deployment:
  tasks:
    - export DEPLOYPATH=/home/cimmytrax/public_html
    - /bin/cp -R dist/* $DEPLOYPATH
    - /bin/cp server.js $DEPLOYPATH
    - /bin/cp package.json $DEPLOYPATH